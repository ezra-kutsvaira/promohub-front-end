# Frontend dev server port (use 5173 to avoid conflict with backend's 8080)
VITE_DEV_PORT=5173

# Option 1 (recommended for deployed/prod frontend): full backend base URL
# Backend docker-compose maps app to localhost:8080
VITE_API_BASE_URL=http://localhost:8080

# Option 2 (recommended for local development): Vite proxy target for /api
# Backend docker-compose maps app to localhost:8080
VITE_API_PROXY_TARGET=http://localhost:8080
